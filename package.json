{
  "name": "groundwork",
  "version": "3.0.0",
  "private": true,
  "description": "Groundwork — the honest tech-stack advisor. Static frontend + Vercel serverless functions.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "scripts": {
    "test": "node --test test/",
    "test:live": "RUN_LIVE=1 node --test test/"
  }
}
